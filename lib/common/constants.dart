import 'package:flutter/material.dart';
import 'package:logger/logger.dart';

/// Use this to debug instead [debugPrint].
final kLogger = Logger();

/// Used as widgets padding.
const kPaddingValue = 16.0;

/// User as widgets spacer (e.g [SizedBox.height] for vertical whitespace).
const kSpacerValue = 16.0;

/// Larger [kSpacerValue].
const kLargeSpacerValue = 24.0;
